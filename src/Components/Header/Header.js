import './Header.css';

function Header() {
    return (
        <header class="topnav">
            <a href="/">Home</a>
            <a href="/profile">Profile</a>
        </header>
    );
}

export default Header;
